name: PR Tests

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Start backend and DB
        run: docker compose up -d --build

      - name: Wait for MySQL to be ready
        run: |
          echo "⏳ Waiting for MySQL to start..."
          for i in {1..20}; do
            docker exec $(docker ps -qf "name=db") \
              mysqladmin ping -h localhost -uroot -proot --silent && break
            echo "❌ MySQL not ready yet, waiting..."
            sleep 3
          done
          echo "✅ MySQL is readyewq!"

      - name: Wait for backend API to be ready
        run: |
          echo "⏳ Waiting for ba2121ckend to start on port 8080..."
          npx wait-on --timeout 60000 tcp:8080
          echo "✅ Backend port is open, checking PHP response..."
          curl -v http://localhost:8080/index.php || true

      - name: Run API tests
        working-directory: ./frontend
        env:
          BACKEND_URL: http://localhost:8080/index.php
        run: npm test

      - name: Show backend logs on failure
        if: failure()
        run: docker compose logs php

      - name: Tear down Docker Compose
        if: always()
        run: docker compose down
