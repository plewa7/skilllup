name: PR Tests

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm install

      - name: Start backend and DB
        working-directory: .
        run: docker compose up -d --build

      - name: Wait for backend API to be ready
        working-directory: .
        # czekamy na root endpoint, który zwraca JSON
        run: npx wait-on --timeout 60000 http://localhost:8080/

      - name: Run API tests
        working-directory: ./frontend
        # BACKEND_URL wskazuje na root endpointewq
        env:
          BACKEND_URL: http://localhost:8080/
        run: npm test

      - name: Tear down Docker Compose
        if: always()
        working-directory: .
        run: docker compose down
